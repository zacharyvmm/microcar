[package]
name = "microcar-plant"
version = "0.1.0"
edition = "2021"
description = "Vehicle plant model for the microcar costar demo"

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
